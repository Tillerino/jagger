package org.tillerino.jagger.tests.variants;

/**
 * Does not apply {@link Variant} to this element, but keeps this element and applies any configured variants to the
 * children of this element.
 */
public @interface ApplyVariantsToChildren {}

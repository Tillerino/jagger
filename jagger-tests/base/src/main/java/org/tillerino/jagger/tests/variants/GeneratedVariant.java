package org.tillerino.jagger.tests.variants;

/** Added by {@link GenerateVariants} to generated variants so that they can be detected by later runs. */
public @interface GeneratedVariant {
    String value() default "";
}
